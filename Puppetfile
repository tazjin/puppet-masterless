#!/usr/bin/env ruby
# This file contains Puppet dependencies that will be installed from the Puppet
# forge using librarian-puppet

forge "https://forgeapi.puppetlabs.com"

# Puppetlabs' stdlib package contains several useful things
mod 'puppetlabs-stdlib'
